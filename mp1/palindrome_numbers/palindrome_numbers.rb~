reverse = 0
temp = 0
puts "Enter the number"
no = gets.chomp.to_i
temp = no
while temp != 0 do
    reverse = reverse * 10
    reverse = reverse + temp%10
    temp = temp/10
end
 if no == reverse
    puts "The number #{no} is a palindrome"
 else
     puts "The number #{no} is not a palindrome"
end
